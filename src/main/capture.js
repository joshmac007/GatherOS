// Screenshot capture — implemented in Session 2.
// Will handle global hotkey registration, transparent overlay window,
// region selection, and saving via desktopCapturer.

function registerCaptureHotkey() {
  // no-op for scaffold
}

function unregisterCaptureHotkey() {
  // no-op for scaffold
}

module.exports = {
  registerCaptureHotkey,
  unregisterCaptureHotkey,
};
