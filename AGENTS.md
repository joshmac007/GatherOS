# Archived contribution checkout

- Do not continue development in this checkout.
- Existing work is published on `contrib/x-bookmark-social-import`.
- New contributions use temporary `contrib/<feature>` branches from
  `upstream/main` inside the canonical `GatherLocal` checkout.
- Never bring local-only AI, server replacements, entitlement changes, config,
  secrets, or user data into Brett-facing contributions.
