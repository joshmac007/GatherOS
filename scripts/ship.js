#!/usr/bin/env node
// Public shipping is intentionally disabled for GatherLocal until the repo,
// license, signing, and release target are confirmed.

console.error('\nGatherLocal public ship is disabled. Use `npm run release` for local --publish never builds.\n');
process.exit(1);
